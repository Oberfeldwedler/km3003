@startuml
!theme plain

class MySql{
        __connectionState
        hostAddress
        portNumber
        username
        password
        database
        connection
        __init__(self, mySqlSettingsDict)
        getConnectionState(self)
        establishConnection(self)
        closeConnection(self)
        ensureDatabaseConnection(self)
        getUserFromDatabase(self, barcode)
        getProductFromDatabase(self, barcode)
        runBarcodeAgainstDatabase(self, barcode)
        getUserBalance(self, user)
        calculateUserBalance(self, user)
        updateUserBalance(self, user, new_balance)
        insertCheckout(self, products_list, user)
        calculateAndUpdateUserBalance(self, user)
        insertPurchasesList(self, products_list, user)
        insertPurchase(self, product, user)
}


class User{
        id
        barcode
        name 
        __init__(self, id, barcode, name)
}

class Product{
        id
        barcode
        name 
        price
        sequential_product_row_number
        __init__(self, id, barcode, name, price)
        generateRow(self)
}

class ShoppingCart{
        database_caller
        products_list[]
        user
        __init__(self, database_caller)
        reset(self)
        checkout(self)
        removeProductByRowNumber(self, row_number)
        refreshUser(self)
}


ShoppingCart o-- Product
ShoppingCart *-- User
ShoppingCart *-- MySql


@enduml