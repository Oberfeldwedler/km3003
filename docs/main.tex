
\documentclass[12pt]{article}


\usepackage{caption}
\usepackage{float}

\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,      
    urlcolor=cyan,
    pdftitle={Technische Dokumentation Kühlschrankmanager KM3003},
    pdfpagemode=FullScreen,
    }


\usepackage{acronym}
\usepackage{graphicx}

\usepackage{xcolor}
% Code highlighting
\NewDocumentCommand{\codeword}{v}{%
\texttt{\textcolor{darkgray}{#1}}%
}

\usepackage{biblatex}
\addbibresource{./VMCB.bib} %Import the bibliography file

\graphicspath{ {./resources} }

\author{Samuel Brunner}
\date{\today}
\title{Technische Dokumentation Kühlschrankmanager KM3003}

\begin{document}

\maketitle

\newpage
\tableofcontents

\newpage

\section{Einleitung}
Der Kühlschrankmanager im Vertretungszimmer der Fachschaft ersetzt die Strichliste, welche früher am Kühlschrank hing, und die Getränkekäufe der Vereinsmitglieder erfasste.

Früher wurden auf der Liste für jedes erworbene Getränk Striche in der Zeile des jeweiligen Mitglieds und in der Spalte des Getränks eingetragen. Regelmäßig zählte man die Striche, ermittelte so die Saldos der Mitglieder und erstellte eine neue Liste.

Der KM3003 ist die Weiterentwicklung der bisherigen Kühlschrankmanager (KM3000 bis KM3002). Der KM3002 basierte auf einem Raspberry Pi mit Touchscreen und NFC-Reader in einem 3D-gedruckten Gehäuse. Mitglieder wurden per Studentenausweis mit NFC identifiziert, und Käufe über den Touchscreen ausgewählt. Die Daten wurden in einer MySQL-Datenbank gespeichert, die jedoch nur negative Salden ermöglichte und so exakte Abrechnungen erzwang. Zudem gab es Verbesserungsbedarf in Codequalität, Zuverlässigkeit und Geschwindigkeit.

Der KM3003 erlaubt nun die Identifizierung von Mitgliedern und Produkten über das Scannen von Barcodes. Mitglieder werden über die Barcodes auf den Studentenausweisen erkannt, Produkte über EAN-Codes. Außerdem erlaubt er nun auch positive Saldos, sodass Mitglieder flexibel Guthaben einzahlen können.

Zusammengefasst ist der KM3003 ein Self-Checkout Point-of-Sale-Gerät mit Touchscreen und Barcode-Scanner.

\section{Plattform und Systemarchitektur}
Als Basis für den Kühlschrankmanager wurde ein Microsoft Surface 3 mit dem zugehörigen Dock verwendet.
Das Surface 3 ist ein Tablet-Computer mit einem zuverlässigen kapazitativen Multi-Touchscreen und solider Treiberunterstützung unter Windows.
Als Betriebssystem wird ein unverändertes Windows 11 Home verwendet, um eine gut getestete und stabile Basis zu bieten. Auch die Verfügbarkeit von Updates in der Zukunft wird so maximiert.

Weiterhin bieten die meisten Microsoft Surface Geräte einen Kioskmodus der spezielle Lösungen für Geräte mit festem und orstunveränderlichem Verwendungszweck bietet.
So optimiert der Kioskmodus das Ladeverhalten für Geräte, die permanent mit Strom versorgt werden, um die Akkulebensdauer zu verlängern.
Weiterhin kann der Zugang zu Anwendungen, die nicht dem gedachten Verwendungszweck entsprechen, beschränkt werden.

Zum scannen wird ein Freihandscanner (Modell DS9208) der Firma Zebra verwendet. Dieser wird per USB angeschlossen, meldet sich aber unter Windows, als ein über serielle Schnittstelle angebundenes Gerät.
Dies erfolgt unter Verwendung des \ac{USB CDC} Treibers.

Das Surface speichert keinerlei Daten. Hierfür wird eine externe MySQL-Datenbank verwendet, welche Nutzer, Produkte, Ein- bzw. Auszahlungen und Einkäufe enthält.

\section{Software}
Als Implementierungssprache wurde objektorientiertes Python3 verwendet. Python ist weit verbreitet und einfach zu lernen, was Wartungsarbeiten oder die Erweiterung mit neuen Features für zukünftige Mitglieder erleichtert. 

Weiterhin können Python Anwendungen auf Linux und Windows Platformen ausgeführt werden, sodass bei Bedarf auch das bisher verwendete Raspberry Pi mit externem Touchscreen weiter verwendet werden könnte. 

Auch die Verfügbarkeit von bestehenden Bibliotheken für \ac{GUI}, Datenbankanbindung und serielle Schnittstellen ist optimal.

\subsection{Verwendete Bibliotheken und Frameworks}

\subsubsection{\ac{GUI} Framework}

Bei der Auswahl eines GUI-Frameworks für Python-Projekte bieten PyQt, Tkinter und PySimpleGUI unterschiedliche Stärken und Einschränkungen in Bezug auf Lizenzmodell, Dokumentation und Architektur. 

Tkinter und PyQT bieten Python Schnittstellen zu den externen GUI toolkits Tk und Qt. PySimpleGUI hingegen baut auf solchen toolkits auf und legt eine weitere Abstraktionsebene darüber um das Erstellen von GUIs über diese Toolkits hinweg zu vereinheitlichen und weiter zu vereinfachen.

PySimpleGUI unterliegt seit Version 5 einer projekteigenen Lizenz, welche die kostenlose Nutzung für Hobbyentwickler und Bildungszwecke erlaubt, sofern keine kommerzielle Nutzung stattfindet. Tkinter und PyQT sind GPL kompatibel bzw. direkt mit GPL linenziert. Bei Tkinter und PyQt ist nicht davon auszugehen, dass sich an der Lizenzierung etwas ändert. Bei PySimpleGUI hingengen muss jedes Jahr wieder eine neue Hobbyisten- Lizenz über einen Account gelöst und wieder im Projekt eingepflegt werden. In Anbetracht der bisherigen Änderungen und dem zusätzlichen Arbeitsaufwand die Lizenz jährlich zu aktualisieren ist PySimpleGUI lizenztechnisch die schlechteste Wahl.

Die Dokumentation der drei Frameworks variiert stark in der Zielgruppe und Benutzerfreundlichkeit. PySimpleGUI ist stark auf Hobbyentwickler ausgelegt und verwendet viele Beispielprogramme, um Funktionen zu erklären. Tkinter und PyQT bieten eine standardisierte Dokumentation, die weniger auf Anfänger zugeschnitten ist, aber erfahrenen Entwicklern eine effizientes Nachschlagewerk bietet.

Architektonisch ähneln sich die Frameworks in der Grundlegenden Handhabung ihrer Komponenten. Alle genannten Frameworks arbeiten mit Elementen, welche durch Python Objekte repräsentiert werden. Diese Objekte haben Parameter und können durch die Nutzung von Hierarchien und Geometriemanagern positioniert werden.

Die Beispielorientierte Dokumentation von PySimpleGUI ermöglicht schnelle erste Ergebnisse und macht es für Einsteiger sehr zugänglich. Aus diesem Grund wurde zu Beginn dieses Projektes PySimpleGUI als Framework ausgewählt, was sich jedoch schnell als Fehlentscheidung herausstellte. Die unstrukturierte Dokumentation wird schnell zum Hindernis und auch das Lizenzmodell steht, wie oben erklärt, PyQt und Tkinter nach. Für das Projekt KM3004 wird vorraussichtlich Tkinter verwendet.


\begin{figure}[ht]
    \includegraphics[width=1\textwidth]{sample.jpg}
    \caption{Das \ac{GUI} von KM3003}
    \label{fig:gui}
\end{figure}

\subsubsection{Datenbank-Connector}
Für die Anbindung einer MySQL Datenbank gibt es verschiedene Optionen welche verwendet werden können, die sich aber für diesen Anwendungsfall nur geringfügig unterscheiden. Der von Oracle selbst entwickelte MySQL Python Connector und PyMySQL sind beide PEP249 kompatibel, sodass sich deren API meist nur im Namen der Methoden unterscheiden. In disem Projekt wurde PyMySQL verwendet, da ich Community-Lösungen gegenüber Angeboten von kommerziellen Anbietern bevorzuge.  \cite{MysqlconnectorpythonSelfcontainedPython} \cite{PyMySQLPurePython}


\subsection{Datenbankstruktur}

% TODO: Struktur der Datenbank

\subsubsection{Maßnahmen zur Verhinderung von Inkonsistenzen und Redundanzen}
\label{sec:measures_to_prevent_inconsistencies_and_redundancies}
Bei der Konzeption der Datenbank wurde besonderer Wert auf die Vermeidung von Redundanzen und Inkonsistenzen gelegt. Die Datenbankstruktur wurde so gestaltet, dass logische Regeln, die direkt durch den MySQL-Server abgebildet werden können, konsequent in diesen verlagert wurden.

Erforderliche Spalten wurden als NOT NULL definiert, während nicht zwingend erforderliche Spalten mit sinnvollen Default-Werten versehen wurden. Dies gewährleistet, dass bei der Erstellung unvollständiger Datensätze automatisch ein Fehler ausgelöst wird. Synthetische Primärschlüssel werden durch Auto-Inkrement automatisch generiert, was die Konsistenz der Primärschlüssel sicherstellt.

Die Wahl der Datentypen erfolgte so restriktiv wie möglich, um eine präzise und fehlerfreie Datenspeicherung zu ermöglichen. Beispielsweise sind für Eurobeträge maximal zwei Dezimalstellen erlaubt, wodurch unnötige Abweichungen ausgeschlossen werden.

Zudem werden mithilfe von DEFAULT-Definitionen und Expressions bestimmte Spalten automatisch mit Werten befüllt. Beispielsweise werden Timestamps direkt vom MySQL-Server gesetzt, wodurch diese bei einer INSERT-Query nicht explizit angegeben werden müssen. Dies schafft eine einheitliche Zeitreferenz (Single Source of Truth) und minimiert potenzielle Fehlerquellen, die durch abweichende Systemzeiten auftreten könnten.

Außerdem erfüllt die Datenbank die Normalisierungsanforderungen bis zur \ac{5NF} vollständig. ??? Wirklich ???

Durch diese Maßnahmen wird eine hohe Datenqualität und Konsistenz sichergestellt, während gleichzeitig Verfügbarkeit der Daten für weitere Verarbeitung maximiert wird.

\subsection{Die Hauptschleife der Anwendung}
Die Hauptschleife der Anwendung bildet das funktionale Kernsegment von KM3003. Wie in Abbildung \ref{fig:main_loop_km3003} gezeigt, wird zu Beginn der Hauptschleife für eine Sekunde auf neue Ereignisse gewartet. Im weiteren Verlauf der Hauptschleife, reagiert die Anwendung dann auf diese Ereignisse.
Neben Eingaben der Benutzer, können auch abgelaufene Timer, Änderungen im Verbindungsstatus zu Peripheriegeräten, oder andere interne Vorgänge Ereignisse auslösen.
Ist dies der Fall, so werden die von der Anwendung selbst ausgelösten Ereignisse, wiederum in der Hauptschleife mit \codeword{window.write_event_value('-EVENT-', None)} ausgelöst und zu Beginn des nächsten Durchlaufs der Schleife, mit \codeword{event, values = window.read(timeout=1000)}, gelesen und danach verarbeitet.

\begin{figure}[H]
    \includegraphics[width=1\linewidth]{main_loop.pdf}
    \caption{Hauptschleife von KM3003}
    \label{fig:main_loop_km3003}
\end{figure}

\subsection{Klassen}

\begin{figure}[ht]
    \includegraphics[width=1\linewidth]{class_diagram.png}
    \caption{Klassendiagramm von KM3003}
    \label{fig:class_diagram}
\end{figure}

\subsubsection{ShoppingCart}
Im ShoppingCart werden Objekte gespeichert, welche die Produkte im Warenkorb und den Nutzer, der sich als letztes eingescannt hat, repräsentieren. Auch eine Instanz der Klassen MySql für die Kommunikation mit dem MySQL-Server wird vom ShoppingCart verwaltet.
Alle für den Nutzer auf der Benuteroberfläche verfügbare Funktionen, wurden direkt in den Methoden des ShoppingCart Objektes implementiert. Funktionen, welche Datenbankabfragen erfordern, werden durch weitere Funktionsaufrufe am MySQL-Caller-Objekt initiiert.
Beim Start des KM3003 wird eine Instanz der Klasse ShoppingCart erstellt welche bis zum Beenden der Anwendung bestehen bleibt.

Objekte der gescannten Produkte oder des gescannten Nutzers, werden in der Hauptschleife der Anwendungen erstellt und im ShoppingCart Objekt gespeichert.

% \subsection{Testing}
% Unit tests schreinben mit pyunit test Framework

\subsubsection{MySql}
\label{sec:mysql}
Die Instanz eines Objektes der Klasse MySql verwaltet die Verbindung zur Datenbank und führt alle Operationen durch, in welchen Daten mit der Datenbank ausgetauscht werden. 

Ider hauptschleife der Anwendung wird die Funktion \hyperref[func:ensureDatabaseConnection]{ensureDatabaseConnection}  periodisch aufgerufen und damit der aktuelle Verbindungsstatus abgefragt.
Ist die Verbindung unterbrochen löst die Funktion \hyperref[func:ensureDatabaseConnection]{ensureDatabaseConnection} einen erneuten Verbindungsversuch aus, sodass die Verbindung zur Datenbank automatisch wiederhergestellt wird. Der initiale Verbindungsaufbau funktioniert ebenfalls über diesen Mechanismus.

Im Allgemeinen sind Datenbankanfragen immer nach dem gleichen Schema implementiert. Der Rohbau einer Query steht direkt im Code als String. Variable Elemnte der Abfragen werden vorerst durch den Platzhalter \codeword{%s} repräsentiert. 
Die Funktion \codeword{execute(query, args)} baut dann aus dem Rohbau der Query und einem Tupel, welches die einzusetzenden Werten enthält, die fertige Query und führt sie aus. 
Bei diesem Ansatz platziert PyMySQL automatisch Escape-Charcters vor Sonderzeichen in den Argumenten für die Query und ist somit gesichert gegen Injection-Angriffe.

Über die Funktion \hyperref[func:runBarcodeAgainstDatabase]{runBarcodeAgainstDatabase} kann die Datenbank nach einem gescannten Barcode durchsucht werden. Falls eine Entität mit dem Barcode eindeutig identifiziert wurde, wird ein Objekt zurückgegeben, welches alle Informationen zur Entität enthält. Hierbei ist es unerheblich ob es sich bei der Entität um einen Nutzer oder ein Produkt handelt.

Wie in Kapitel \ref{sec:measures_to_prevent_inconsistencies_and_redundancies} erklärt, wurde bei der Konzeption dieses Projektes explizit darauf geachtet, die Datenbank als Single Source of Truth zu respektieren. Deswegen wird das Guthaben eines Users bei jeder Verwendung über die Funktion \hyperref[func:getUserBalance]{getUserBalance} aus der Datenbank geladen.

Die Guthaben der Nutzer werden über die Funktion calculateAndUpdateUserBalance lokal durch Addition aller Einzahlungen und Subtraktion aller Einkäufe berechnet.

Da sichergestellt ist, dass die Daten in der Datenbank immer aktuell sind, könnten in der Zukunft auch weitere Kühlschrankmanager oder Clients zur Administration gleichzeitig eingesetzt werden.

\subsubsection{Scanner}
Der Scanner wurde mit einem Producer-Consumer Modell realisiert. 

Die Funktion \hyperref[func:readFromScanner]{\_\_readFromScanner} wird in einem seperaten Thread ausgeführt und blockiert solange bis ein Barcode erkannt wird. Der Wert des Barcodes wird dann hinten an eine Queue angereiht und in der Hauptschleife der Anwendung über die Funktion \hyperref[func:getBarcode]{getBarcode} konsumiert.

Da das Modul pySerial, um den Status der seriellen Schnittstelle zu überprüfen, keine bequeme Ping Funktion wie by PyMySQL bietet, wurde hier ein anderer Ansatz gewählt.
Bei der Initialisierung des Scanner Objektes wird eine Callback Funktion hinterrlegt, welche bei jeder Änderung des Verbindungsstatus zum Scanner aufgerufen wird.
Als Argument wird hier der neue Verbindungsstatus übergeben.

Der Wiederaufbau der Verbindung selbst wird in der Funktion \hyperref[func:readFromScanner]{\_\_readFromScanner} gestartet, falls \_\_connectionState nicht den Wert "up" hat.

\subsection{Logging}

Implementiert von Hannes Bachl und Samuel Brunner. Dokumentiert von Samuel Brunner.

Um eine effiziente Fehlerdiagnose zu ermöglichen, ist gutes Logging unabdingbar. Die Logfiles werden im Ordner \codeword{./Logs} relativ zur Anwendung gespeichert. 
Sollte der Ordner nicht existieren, so wird er automatisch erstellt. 
Durch die Verwendung eines RotatingFileHandlers, wird nach jedem Start der Anwendung ein neues Logfile angelegt. Es werden die vergangenen sieben Logfiles aufgehoben.
Da in der Anwendung auch Emojis unterstützt werden, muss als Encoding UTF-8 spezifiziert werden.

Wie in Kapitel \ref{sec:mysql} erklärt, werden Datenbank-Queries als String mit Platzhaltern gehardcoded. Die Funktion \codeword{execute(query, args)} des Moduls PyMySQL ersetzt dann die Platzhalter mit den richtigen Werten und führt sie aus. Die Funktion \codeword{mogrify(query, args)} besitzt den gleichen Funktionsprototypen, führt jedoch keine Datenbank Transaktion durch. Stattdessen ersetzt \codeword{mogrify(query, args)} die Platzhalter ebenso mit den Werten aus dem Tupel und gibt die fertige Datenbanktransaktion als String zurück, so wie sie von \codeword{execute(query, args)} ausgeführt worden wäre. Dieser String kann dann geloggt werden.

\subsection{Konfiguration}
Die Konfiguration der Anwendung erfolgt über das Konfig-file \codeword{./km3003.conf}. Die Sprache welche vom Modul \codeword{configparser} verwendet wird, ist sehr stark angelehnt an, aus Microsoft Windows bekannten, \codeword{*.INI} Dateien.

Es gibt keine optionalen Parameter. Die Konfigurationsdatei muss immer alle Optionen enthalten.

Die vollständige Dokumentation aller Konfigurationsparameter befindet sich im Anhang in Teil \ref{sec:documentation_of_config_parameters}. 

Die Ausdrücke \codeword{true}, \codeword{1}, \codeword{t}, \codeword{y}, \codeword{yes}, \codeword{yeah}, \codeword{yup}, \codeword{ja}, \codeword{jawoll}, \codeword{definitiv} werden als \codeword{True} gewertet. Alles andere als \codeword{False}.

\section{Deployment}

Die Unterstützung für Herangehensweisen welche \ac{IaC} nutzen ist unter Windows sehr schlecht. Der notwendige Arbeitsaufwand für eine vollständige Automatisierung des Setups, in Anbetracht der sehr wenigen notwendigen Installationsschritte, wäre zu groß. Deswegen werden die durchzuführenden Installationsschritte hier und im globalen README des Projekts dokumentiert.

\begin{enumerate}
    \item Install Python
    \begin{itemize}
        \item do NOT install als admin
        \item set the checkbox to add to PATH
    \end{itemize}
    \item install python libraries
    \begin{verbatim}
python -m pip install pysimplegui pyserial pymysql cryptography
    \end{verbatim}
    \item setup km3003.conf
    \begin{itemize}
        \item make a copy of km3003\_sample.conf and name it km3003.conf
        \item customize database info
        \item customize COM port
    \end{itemize}
    \item obtain PySimpleGUI Hobbyist license
    \item Setup Windows Task Scheduling to reboot the system periodically
\end{enumerate}

\section{Ausblick und Verbesserungsvorschläge}
    Das kommerzialisierte Lizenzmodell und die auf Anfänger ausgerichtete Dokumentation, machen PySimpleGUI zur schlechtesten Wahl unter den drei diskutierten Frameworks. 
    In nachfolgenden Versionen wird eine Umstellung auf Tkinter oder PyQt erfolgen.
    
    Bisher werden neue Benutzer oder Produkte direkt in die Datenbank über das Programm MySQL Workbench eingetragen. Mithilfe eines dedizierten Clients zur Administration von Entities und Events welche in der Datenbank gespeichert sind, könnten diese Aufgaben komfortabler und sicherer gemacht werden.
    Eine weitere Verarbeitung und Darstellung der in der Datenbank gespeicherten Daten ist geplant. Hierfür sehe ich eine einfache Anwendung in Matlab oder eine Excel Tabelle als am geeignetsten an. Ein gehostetes Dashboard ist nicht vorgesehen, da die personenbezogenen Daten nicht jederezeit uneingeschränkt zugänglich sein müssen oder gar sollen.


\appendix

\section{Inline documetation}

\subsection{MySql}

\subsubsection{ensureDatabaseConnection(self)}
\label{func:ensureDatabaseConnection}

\begin{verbatim}
"""
Checks whether there is a working database connection 
and reestablishes when there isn't.

Args: 
    None
Returns:
    connectionState: The connection state to the MySql server
    connectionStateChanged: This indicates, whether the connection
        state is now different to the one specified in 
        __connectionState.
        This information is used by the GUI to update the layout.
"""
\end{verbatim}

\subsubsection{runBarcodeAgainstDatabase(self, barcode)}
\label{func:runBarcodeAgainstDatabase}

\begin{verbatim}
"""
Searches in the database for users or products with the 
supplied barcode. If an entity can be identified by the 
code, an object containing the fetched information about
the entity is returned.

Args: 
    Barcode: The content of a scanned barcode.
Returns:
    Entity: Object representing a user or a product. 
        None if no matching entity is found in the database. 
"""
\end{verbatim}

\subsubsection{getUserBalance(self, user)}
\label{func:getUserBalance}

\begin{verbatim}
"""
Fetches the current user_balance from the database.
Args:
    user: An instance if a user oject.
Returns:
    user_balance: The current balance of the user.
    None if no matching user is found in the database.
"""
\end{verbatim}


\subsection{Scanner}

\subsubsection{\_\_constructSerialConnection(self)}
\label{func:constructSerialConnection}
\begin{verbatim}
"""
This function configures the serial connection, using the
settings in the dictionary supplied on initialization.
The serial connection is not opened here.

Args: 
    None
Returns:
    ser: The configured serial connection as an object.
"""
\end{verbatim}

\subsubsection{\_\_openSerialConnection(self)}
\label{func:openSerialConnection}
\begin{verbatim}
"""
This function opens the serial connection.
After successfully opening the connection, __connectionState 
is set to "up" and the callback function is executed.
In case of an exception, the function sleeps, to limit 
connection attempts to one per second.

Args: 
    None
Returns:
    None
"""
\end{verbatim}

\subsubsection{\_\_readFromScanner(self)}
\label{func:readFromScanner}
\begin{verbatim}
"""
This Function tries to read a line from the 
scanner while no stop is requested. As long
as there is no input from the scanner, 
self.ser.readline() is blocking.
In case self.ser.readline() throws an 
exception, the connectionState is set to "down"
and the callback function is executed.

Args: 
    None
Returns:
    None
"""
\end{verbatim}

\subsubsection{getBarcode(self)}
\label{func:getBarcode}
\begin{verbatim}
"""
This Function checks whether there are elements in the queue.
In case there are, it fetches the oldest element and returns it.
In case there are none, None is returned.

Args: 
    None
Returns:
    item:
        String containing the value of a scanned barcode.
"""
\end{verbatim}

% \section{Dokumentation der Konfigurationsparameter}
% \codeword{general \ screen_timeout_ms}: Zeit in Millisekunden, bis der Bildschirmschoner aktiviert wird.\\
% \codeword{general \ message_timeout_ms}: Dauer in Millisekunden, wie lange Nachrichten auf dem Bildschirm angezeigt werden.\\
% \codeword{general \ theme}: Farbschema des Benutzerinterfaces. Mögliche Werte sind aus der PySimpleGUI Dokumenation zu entnehmen.\\
% \codeword{general \ font}: Schriftart, die in der Benutzeroberfläche verwendet wird.\\
% \codeword{general \ font_size}: Schriftgröße in Punkten für die Benutzeroberfläche.\\
% \codeword{general \ initial_width}: Anfangsbreite des Fensters in Pixeln.\\
% \codeword{general \ initial_height}: Anfangshöhe des Fensters in Pixeln.\\
% \codeword{general \ keep_on_top}: Gibt an, ob das Fenster immer im Vordergrund bleiben soll.\\
% \codeword{general \ logging}: Gibt das loglevel an. Der Wert \codeword{debug} bedeutet, dass detaillierte Debugging-Informationen protokolliert werden.\\
% \codeword{general \ debug}: Äquivalent zu \codeword{resizable = True}, \codeword{no no_titlebar = False}, \codeword{maximize = False}\\
% \codeword{general \ resizable}: Gibt an, ob die Größe des Fensters geändert werden kann. \codeword{False} bedeutet, dass das Fenster nicht skalierbar ist.\\
% \codeword{general \ no_titlebar}: Gibt an, ob das Fenster eine Titelleiste haben soll. \codeword{True} bedeutet, dass keine Titelleiste angezeigt wird.\\
% \codeword{general \ maximize}: Falls auf \codeword{True} gesetzt, wird das Fenster beim Start maximiert.\\
% \\
% \codeword{mysql \ host_address}: Die Adresse des MySQL-Hosts, zu dem eine Verbindung hergestellt werden soll.\\
% \codeword{mysql \ port_number}: Der Port, über den die Verbindung zum MySQL-Host hergestellt wird.\\
% \codeword{mysql \ username}: Der Benutzername für die MySQL-Verbindung.\\
% \codeword{mysql \ password}: Das Passwort für die MySQL-Verbindung.\\
% \codeword{mysql \ database}: Der Name der MySQL-Datenbank, die verwendet werden soll.\\
% \\
% \codeword{serial \ port}: Der COM-Port oder das Gerät, das für serielle Kommunikation verwendet wird. Unter Linux \codeword{/dev/ttyXYZ}\\
% \codeword{serial \ console_input}: Der Input erfolgt nicht über die serielle Schnittstelle, sondern über die Konsole.\\
% \codeword{serial \ baudrate}: Die Baudrate für die serielle Kommunikation.\\
% \codeword{serial \ bytesize}: Die Anzahl der Datenbits pro Zeichen.\\
% \codeword{serial \ parity}: Gibt das Paritätsbit für die serielle Kommunikation an.\\
% \codeword{serial \ stopbits}: Die Anzahl der Stopbits für die serielle Kommunikation.\\

\section{Dokumentation der Konfigurationsparameter}

\label{sec:documentation_of_config_parameters}
\subsection{General}
\begin{itemize}
    \item \textbf{\texttt{screen\_timeout\_ms}}: Zeit in Millisekunden, bis der Bildschirmschoner aktiviert wird.
    \item \textbf{\texttt{message\_timeout\_ms}}: Dauer in Millisekunden, wie lange Nachrichten auf dem Bildschirm angezeigt werden.
    \item \textbf{\texttt{theme}}: Farbschema des Benutzerinterfaces. Mögliche Werte sind aus der PySimpleGUI Dokumentation zu entnehmen. \cite{ThemesPySimpleGUIDocumentationc}
    \item \textbf{\texttt{font}}: Schriftart, die in der Benutzeroberfläche verwendet wird.
    \item \textbf{\texttt{font\_size}}: Schriftgröße in Punkten für die Benutzeroberfläche.
    \item \textbf{\texttt{initial\_width}}: Anfangsbreite des Fensters in Pixeln.
    \item \textbf{\texttt{initial\_height}}: Anfangshöhe des Fensters in Pixeln.
    \item \textbf{\texttt{keep\_on\_top}}: Gibt an, ob das Fenster immer im Vordergrund bleiben soll.
    \item \textbf{\texttt{logging}}: Gibt das Loglevel an. Der Wert \texttt{debug} bedeutet, dass detaillierte Debugging-Informationen protokolliert werden.
    \item \textbf{\texttt{debug}}: Bei \texttt{True} äquivalent zu \texttt{resizable = True}, \texttt{no\_titlebar = False}, \texttt{maximize = False}.
    \item \textbf{\texttt{resizable}}: Gibt an, ob die Größe des Fensters geändert werden kann. \texttt{False} bedeutet, dass das Fenster nicht skalierbar ist.
    \item \textbf{\texttt{no\_titlebar}}: Gibt an, ob das Fenster eine Titelleiste haben soll. \texttt{True} bedeutet, dass keine Titelleiste angezeigt wird.
    \item \textbf{\texttt{maximize}}: Falls auf \texttt{True} gesetzt, wird das Fenster beim Start maximiert.
\end{itemize}

\subsection{MySQL}
\begin{itemize}
    \item \textbf{\texttt{host\_address}}: Die Adresse des MySQL-Hosts, zu dem eine Verbindung hergestellt werden soll.
    \item \textbf{\texttt{port\_number}}: Der Port, über den die Verbindung zum MySQL-Host hergestellt wird.
    \item \textbf{\texttt{username}}: Der Benutzername für die MySQL-Verbindung.
    \item \textbf{\texttt{password}}: Das Passwort für die MySQL-Verbindung.
    \item \textbf{\texttt{database}}: Der Name der MySQL-Datenbank, die verwendet werden soll.
\end{itemize}

\subsection{Serial}
\begin{itemize}
    \item \textbf{\texttt{port}}: Der COM-Port oder das Gerät, das für serielle Kommunikation verwendet wird. Unter Linux \texttt{/dev/ttyXYZ}.
    \item \textbf{\texttt{console\_input}}: Der Input erfolgt nicht über die serielle Schnittstelle, sondern über die Konsole.
    \item \textbf{\texttt{baudrate}}: Die Baudrate für die serielle Kommunikation.
    \item \textbf{\texttt{bytesize}}: Die Anzahl der Datenbits pro Zeichen.
    \item \textbf{\texttt{parity}}: Gibt das Paritätsbit für die serielle Kommunikation an.
    \item \textbf{\texttt{stopbits}}: Die Anzahl der Stopbits für die serielle Kommunikation.
\end{itemize}

\section{Abkürzungsverzeichnis}
\begin{acronym}[OTH R]
    \acro{USB CDC}{USB communications device class}
    \acro{GUI}{graphical user interface}
    \acro{IaC}{Infrastructure-as-Code}
    \acro{1NF}{erste Normalform}
    \acro{2NF}{zweite Normalform}
    \acro{3NF}{dritte Normalform}
    \acro{4NF}{vierte Normalform}
    \acro{5NF}{fünfte Normalform}
\end{acronym}

\clearpage

\listoffigures

\clearpage

\printbibliography

\clearpage
\end{document}